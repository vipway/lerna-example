{
  "packages": [
    "packages/*"
  ],
  "version": "0.0.2",
  "command": {
    "publish": {
      "message": "chore(release): publish %v",
      "ignoreChanges": [
        "*.md",
        ".npmrc",
        "package.lock",
        "yarn.lock",
        ".eslintrc",
        ".eslintignore",
        ".gitignore"
      ]
    }
  }
}
